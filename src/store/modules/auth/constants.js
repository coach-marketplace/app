import { SUCCESS, FAILED, LOADING } from "../../../helper/constants";

export const AUTO_LOGIN_FAILED = `AUTO_LOGIN${FAILED}`;
export const AUTO_LOGIN_LOADING = `AUTO_LOGIN${LOADING}`;
export const AUTO_LOGIN_SUCCESS = `AUTO_LOGIN${SUCCESS}`;
export const LOGIN_FAILED = `LOGIN${FAILED}`;
export const LOGIN_LOADING = `LOGIN${LOADING}`;
export const LOGIN_SUCCESS = `LOGIN${SUCCESS}`;
export const LOGOUT = `LOGOUT`;
